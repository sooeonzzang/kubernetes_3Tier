apiVersion: v1
kind: ConfigMap
metadata:
  namespace: metallb-system
  name: config
data:
  config: |
    address-pools:
    - name: default
      protocol: layer2
      addresses:
      - 10.100.0.105-10.100.0.110
    neighbors:
    - addr: fe80::/64
      interface: enp0s3
      disabled: true